# 公开版 v0.3.7.4

版本号：`0.3.7.4` / `30704`。

本版在公开版中加入“小金库”模块：

- 守护页新增小金库入口。
- 小金库支持本地账本、预算规则、待审批、历史月份侧边栏、统计报告。
- 所有“等待审批/审批详情”的名字均读取设置里的陪伴者名称，不写死固定名字。
- MCP 新增小金库工具：读取当前月、读取月份摘要、添加账单、提交花钱申请、读取待审批、审批申请、确认待确认账单、读取/设置预算规则。
- 默认本地保存账单；公开部署不会读取支付密码、不会接管银行卡、不会默认上传完整账单。

